TRUSTSECO_PORT=3002
# local preserves configured Docker peers; shared defaults to the two Utrecht seeds.
TRUSTSECO_NETWORK=local
# Public peer endpoint for this node, used to omit itself from the seed list.
TRUSTSECO_NODE_ADDRESS=
# Optional comma-separated host:port override.
TRUSTSECO_SEED_PEERS=
GH_USERNAME=
GITHUB_TOKEN=
LIBRARIESIO_TOKEN=
